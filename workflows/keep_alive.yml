name: Keep Chrona Awake

on:
  schedule:
    # This cron expression runs the job every 12 minutes, 24/7
    - cron: '*/12 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send keep-alive ping
        run: curl -I https://chrona-rp5z.onrender.com/
